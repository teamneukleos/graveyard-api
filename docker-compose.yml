services:
  db:
    image: postgres:16-alpine
    container_name: graveyard-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: root
      POSTGRES_DB: graveyard
    ports:
      - "5434:5432"
    volumes:
      - graveyard_pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d graveyard"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  graveyard_pg_data:
